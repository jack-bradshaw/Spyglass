apply plugin: 'com.android.library'

android {
	compileSdkVersion 25
	buildToolsVersion "25.0.2"

	defaultConfig {
		minSdkVersion 9
		targetSdkVersion 25
	}
}

dependencies {
	compile project(':markers')
	compile 'com.android.support:appcompat-v7:25.3.1'
}

ext {
	POM_ARTIFACT_ID_SUFFIX = '-runtime'
	POM_NAME = 'Spyglass Runtime'
	POM_PACKAGING = 'aar'
	POM_DESCRIPTION = 'Runtime classes for the Spyglass framework.'
}

apply from: rootProject.file('maven-publish.gradle')