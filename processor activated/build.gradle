apply plugin: 'java'

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

ext {
	PUBLISH_ARTIFACT_ID_SUFFIX = '-processor'
}

dependencies {
	compile project(':processor')
	compile 'com.google.auto.service:auto-service:1.0-rc3'
}

apply from: 'build_release.gradle'